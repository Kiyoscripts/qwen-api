"use client";
import { useState } from "react";

export function LoginForm() {
  const [register, setRegister] = useState(false); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [invite, setInvite] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function submit(e: React.FormEvent) { e.preventDefault(); setBusy(true); setError(""); const res = await fetch(register ? "/api/auth/register" : "/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username, password, invite }) }); const body = await res.json().catch(() => ({})); setBusy(false); if (!res.ok) return setError(body.error || "Unable to continue."); window.location.href = register ? "/account" : body.user?.role === "admin" ? "/admin" : "/account"; }
  return <div className="rounded-[28px] border border-white/10 bg-[#10151d]/90 p-7 shadow-2xl backdrop-blur-xl md:p-9"><div className="mb-8"><p className="mb-2 text-xs font-semibold uppercase tracking-[.24em] text-emerald-400">Private access</p><h1 className="text-3xl font-semibold text-white">{register ? "Claim your invitation" : "Welcome back"}</h1><p className="mt-2 text-sm text-white/55">{register ? "Create your account with a single-use invite." : "Sign in with your username and password."}</p></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm text-white/70">Username<input className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-white outline-none focus:border-emerald-400" value={username} onChange={e => setUsername(e.target.value)} autoComplete="username" required /></label><label className="block text-sm text-white/70">Password<input type="password" className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-white outline-none focus:border-emerald-400" value={password} onChange={e => setPassword(e.target.value)} autoComplete={register ? "new-password" : "current-password"} minLength={10} required /></label>{register && <label className="block text-sm text-white/70">Invite code<input className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 font-mono text-white outline-none focus:border-emerald-400" value={invite} onChange={e => setInvite(e.target.value)} required /></label>}{error && <p className="rounded-xl bg-red-500/10 px-4 py-3 text-sm text-red-300">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-emerald-400 px-4 py-3 font-semibold text-[#07110d] disabled:opacity-50">{busy ? "Working..." : register ? "Create account" : "Sign in"}</button></form><button onClick={() => { setRegister(!register); setError(""); }} className="mt-5 w-full text-sm text-white/55 hover:text-white">{register ? "Already registered? Sign in" : "Have an invite? Register"}</button></div>;
}
