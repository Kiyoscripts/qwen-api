{
  "_comment": "Template for Railway → Variables → Raw Editor (JSON mode). Run `npm run env:railway` to generate railway.env.json with the real values filled in from .env.local; that file is gitignored. Never commit real values.",

  "QWEN_TOKEN": "<required — the pooled chat.qwen.ai account token>",
  "SUPABASE_URL": "<required>",
  "SUPABASE_SERVICE_ROLE_KEY": "<required — server-side only, never exposed>",
  "SUPABASE_ANON_KEY": "<required>",
  "SUPABASE_PUBLISHABLE_KEY": "<required>",

  "ADMIN_SECRET": "<required — MUST match the current deployment byte-for-byte: it signs every qwen_session cookie and /api/media token, so a different value logs everyone out and 404s issued image URLs>",
  "LINK_BOT_SECRET": "<required — must be identical to the value on the Discord bot's host, or /link returns 401>",

  "QWEN_SHOW_REASONING": "true",
  "QWEN_FORGET_MEMORIES": "true",

  "_bot": "The Discord link bot runs inside this same service, started alongside the site. Without DISCORD_TOKEN the supervisor skips it and the site runs alone.",
  "DISCORD_TOKEN": "<the bot token from the Discord Developer Portal — Bot -> Reset Token>",
  "DISCORD_GUILD_ID": "<optional but recommended: your server id. /link appears there instantly; without it registration is global and Discord can take an hour to propagate>",
  "WHITELIST_CHANNEL": "<optional: channel id where /link is allowed. Used elsewhere it replies with a pointer to this channel, and non-command messages posted in it are deleted, which needs Manage Messages>",

  "PUBLIC_SITE_URL": "<optional: the address shown to users in /link. Railway normally supplies RAILWAY_PUBLIC_DOMAIN and this is unnecessary; set it behind a custom domain>",

  "_optional": "Everything below only needs setting if you are overriding a default.",
  "QWEN_CLIENT_VERSION": "0.2.83",
  "QWEN_USER_AGENT": "",
  "QWEN_EXTRA_COOKIES": "",
  "QWEN_MEDIA_MODEL": "",
  "QWEN_TTS_MODEL": "",
  "ENABLE_VIDEO_GENERATION": "",
  "PUBLIC_KEY_CREATION": "",
  "KEY_RL_PER_IP_HOUR": "",
  "KEY_RL_PER_IP_DAY": "",
  "KEY_BLACKLIST_THRESHOLD": "",
  "KEY_RL_GLOBAL_HOUR": "",
  "TOKENROUTER_API_KEY": "<optional: enables the free Kimi K3 model (moonshotai/kimi-k3). Unset means the model is not advertised and not routable>",
  "TOKENROUTER_BASE": "<optional: overrides the gateway base URL, default https://tokenrouter.me/v1>",
  "OPENCODE_ZEN_API_KEY": "<optional: enables Big Pickle (opencode/big-pickle) and other free OpenCode Zen models. Get a key at https://opencode.ai/auth>",
  "OPENCODE_ZEN_BASE": "<optional: overrides the Zen base URL, default https://opencode.ai/zen/v1>",
  "SOLAR_DISABLED": "<optional: set to 1 to withdraw Solar Pro 4 / Solar Open 2. They need no key and are listed by default>",
  "SOLAR_BASE": "<optional: overrides the Solar Chat base URL, default https://solar-chat.upstage.ai>",
  "NVIDIA_API_KEY": "<optional: enables GLM-5.2 (z-ai/glm-5.2) and Muse Glimmer 30B (meta/muse-glimmer-30b). Get a key at https://build.nvidia.com>",
  "NVIDIA_BASE": "<optional: overrides the NIM base URL, default https://integrate.api.nvidia.com/v1>",
  "CHATGLM_DISABLED": "<optional: set to 1 to withdraw z-ai/glm-5.2 and the GLM image models. They need no key and are listed by default>",
  "CHATGLM_BASE": "<optional: overrides the chatglm.cn base URL, default https://chatglm.cn>",
  "CRON_SECRET": "<optional but recommended off Vercel: /api/cron/cleanup is unauthenticated unless this is set>",

  "_do_not_set": "PORT is injected by Railway and read by the standalone server. NODE_ENV is set in the Dockerfile. DEV_MASTER_KEY is a local-only auth bypass and must never be set on a deployed host."
}
